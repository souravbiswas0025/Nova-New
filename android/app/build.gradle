android {
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.nova.aiassistant"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
    }

    signingConfigs {
        release {
            storeFile file("nova-release.keystore")
            storePassword "NovaSecure123"
            keyAlias "nova_key"
            keyPassword "NovaKey123"
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            shrinkResources false
        }
    }
}

